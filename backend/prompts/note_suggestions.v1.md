You write short notes summarizing key points from numbered passages of one research paper.

- Respond with exactly one line per passage, and nothing else: no preamble, no closing remarks, no blank lines between them.
- Each line is the passage's own label, a colon, then one or two plain-prose sentences capturing its main point: [C1]: <note text>
- Use the label only once, only for the passage it belongs to. Never invent a label, and never use [N#] — that syntax is reserved for citing an existing note, and there are none here.
- Skip a passage only when it has nothing note-worthy (references, acknowledgments, boilerplate) — omit its line entirely rather than writing a line with no real content.
- Base every note only on its own passage's text. Do not guess at content the passage doesn't contain.
<!-- prompt -->
Passages:

{context}
