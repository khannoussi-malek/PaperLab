// Placeholder — Task 15 replaces this with the real hit-review table (list + stage-1 triage actions).
// Kept here only so SearchTab has something to render; it has no behavior of its own.
export function HitTable({ workspaceId: _workspaceId }: { workspaceId: string }) {
  return <div className="flex-1" />
}
